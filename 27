class Solution:
    def removeElement(self, nums: List[int], val: int) -> int:
        i=0
        for j in range(len(nums)):
            if (nums[j]!=val):
                nums[i]=nums[j]
                i=i+1
        return i
        
        
#使用“双指针”的方法。
        其实，从表面上看，并没有使用指针，只是其思想，python的指针需要看链表的知识。
